We trained a reinforcement learning (RL) policy using the provided reward function and tracked statistical information. This included the overall statistics and performance trends of the individual components in the reward function, as well as global policy metrics such as consecutive_successes and episode length.